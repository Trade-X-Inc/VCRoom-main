/signup /sign-up 301
/accelerators /resources 301
/grants /resources 301
/waitlist /sign-up 301
/app/news /app 301
/app/pipeline /app 301
/app/vc-leads /app 301
/app/leads /app 301
/app/reports /app 301
/app/accelerators /app 301
/docs/security/rls /docs/security/access-control 301
/docs/founders/verification /docs/founders 301
/docs/ai/verification /docs/ai 301
/docs/investors/intake /docs/investors 301
/docs/ai/deal-brief /docs/ai 301
/trust / 301

# /founders and /investors carried live deleted-feature claims (scored/
# stage-tagged pipelines, "AI risk analysis", "AI-curated" target lists —
# none of this exists; see CLAUDE.md's public-surface correctness pass).
# Deleted rather than edited, since both are being replaced by /for-founders
# and /for-investors. Repointed to the real pages now that Step 5 has
# shipped them (previously redirected to / as an interim target).
/founders /for-founders 301
/investors /for-investors 301
/solutions/due-diligence / 301
/solutions/fundraising-crm / 301
/solutions/investor-pipeline / 301
/solutions/raise-1m / 301
/solutions/vc-deal-room / 301

# Public-site migration Group 2 (25 Aug 2026) — /solutions/founders and
# /solutions/investors are the public-site-spec.html audience-page slugs
# for the same audiences /for-founders and /for-investors already serve,
# shipped and register-compliant (PUBLIC-REGISTER.md v2.0, 23 Aug 2026).
# Redirected rather than duplicated, per the confirmed integration decision.
/solutions/founders /for-founders 301
/solutions/investors /for-investors 301

# Public-site migration Group 3 (25 Aug 2026) — /sectors/technology is the
# public-site-spec.html slug for the same content /resources/schedule
# already serves in full (the real technology/seed disclosure schedule,
# CLAUDE.md §20.1 step 2d — only schedule that exists). Redirected rather
# than building a second, thinner technology page.
/sectors/technology /resources/schedule 301

# Public-site migration Group 5 (25 Aug 2026) — same reasoning as
# /sectors/technology above: /schedules/technology-seed is the
# public-site-spec.html slug for the same one real published schedule.
# Redirected rather than duplicating /resources/schedule a third time.
/schedules/technology-seed /resources/schedule 301

# P4: founder nav restructure — old pages consolidated into /app/prepare
/app/profile      /app/prepare#profile        301
/app/documents    /app/prepare#documents      301
/app/advisor      /app/prepare#verification   301
/app/claims       /app/prepare#claims         301
/app/badges       /app/prepare#badges         301
/app/overview     /app                        301
/app/investor/overview    /app/investor                            301
/app/investor/startups    /app/investor/source#watchlist           301
/app/investor/intake      /app/investor/source#intake              301
# The 5 rules below (deal-rooms/diligence/analysis/decisions/portfolio)
# were removed 22 Sep 2026 — stale since the July 2026 P5 nav restructure.
# They redirected to #hash anchors on source.tsx/evaluate.tsx/decide.tsx
# that those files have never actually read (confirmed: zero hash-reading
# code in any of the three). Real, currently-live UI still links straight
# at these paths — nav-structure.ts's sidebar (Deal Analysis, Decisions),
# and evaluate.tsx/decide.tsx's own SectionLinkRows (Due diligence, AI
# analysis, Deal rooms, Decisions, Portfolio) — so every one of those
# real clicks was silently landing on a link-out page instead of the
# intended screen. Live-verified broken (sidebar "Deal Rooms" -> Evaluate
# page, not the real Deal Rooms list) before removing these rules, and
# live-verified fixed after. See LCS_MIGRATION_PLAN.md Group 8.

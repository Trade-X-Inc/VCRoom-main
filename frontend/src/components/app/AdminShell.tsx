// Full-access shell for founders (owners), investors (owners), and admin-role team members.
// Members with manager/analyst/viewer roles are routed to MemberShell instead.
export { AppShell as AdminShell } from "./AppShell";

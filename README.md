
  # VentureRoom OS UI Design

  This is a code bundle for VentureRoom OS UI Design. The original project is available at https://www.figma.com/design/5pBOtt7jQmTbuIsikohBzd/VentureRoom-OS-UI-Design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
